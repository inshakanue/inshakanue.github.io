# 
# GITIGNORE FILE FOR JEKYLL + NODE.JS PORTFOLIO
# ==============================================
# This file specifies which files and directories should be excluded
# from version control. Files listed here will not be committed to Git.
#

# 
# JEKYLL BUILD OUTPUT
# ===================
# These are generated files that should not be committed.
# They are automatically created when building the Jekyll site.
#
_site/                  # Jekyll build output directory
.jekyll-cache/          # Jekyll cache directory
.jekyll-metadata        # Jekyll metadata file

# 
# SASS/SCSS CACHE
# ===============
# Sass cache files are temporary and should not be committed.
#
.sass-cache/            # Sass compilation cache

# 
# NODE.JS DEPENDENCIES
# ====================
# Node modules are large and can be regenerated from package.json.
# Never commit node_modules to the repository.
#
node_modules/           # Node.js dependencies
npm-debug.log*          # NPM debug logs
yarn-debug.log*         # Yarn debug logs
yarn-error.log*         # Yarn error logs
.pnp/                   # Yarn PnP files
.pnp.js                 # Yarn PnP JavaScript

# 
# RUBY/BUNDLER (if using Bundler for Jekyll)
# ===========================================
# Ruby gem dependencies and Bundler cache.
#
vendor/                 # Ruby gems (if using Bundler)
.bundle/                # Bundler cache directory
*.gem                   # Ruby gem files
*.gemfile.lock          # Gemfile lock file

# 
# ENVIRONMENT VARIABLES AND SECRETS
# ==================================
# These files may contain sensitive information like API keys,
# passwords, or private configuration. NEVER commit these!
#
.env                    # Environment variables
.env.local              # Local environment variables
.env.*.local            # Local environment variable files
.env.production         # Production environment variables
.env.development        # Development environment variables
*.key                   # Private key files
*.pem                   # Certificate files
secrets.yml             # Secrets configuration file
_config.local.yml       # Local Jekyll config (if using)

# 
# SYSTEM FILES
# ============
# Operating system generated files that should be ignored.
#
.DS_Store               # macOS Finder metadata
.DS_Store?              # macOS Finder metadata
._*                     # macOS resource fork files
.Spotlight-V100         # macOS Spotlight index
.Trashes                # macOS trash files
Thumbs.db               # Windows thumbnail cache
Thumbs.db:encryptable   # Windows encrypted thumbnail cache
ehthumbs.db             # Windows folder thumbnail cache
Desktop.ini             # Windows folder customization
$RECYCLE.BIN/           # Windows recycle bin
*.swp                   # Vim swap files
*.swo                   # Vim swap files
*~                      # Backup files
.directory              # KDE directory metadata

# 
# IDE AND EDITOR FILES
# ====================
# Files generated by code editors and IDEs.
# Uncomment the ones you use, or keep them all for team compatibility.
#
.vscode/                # Visual Studio Code settings
.idea/                  # IntelliJ IDEA / WebStorm settings
*.sublime-project       # Sublime Text project files
*.sublime-workspace     # Sublime Text workspace files
*.code-workspace        # VS Code workspace files
.project                # Eclipse project files
.classpath              # Eclipse classpath
.settings/              # Eclipse settings

# 
# TEMPORARY AND CACHE FILES
# ==========================
# Temporary files and caches that should not be committed.
#
*.tmp                   # Temporary files
*.temp                  # Temporary files
.cache/                 # Cache directories
*.cache                 # Cache files
tmp/                    # Temporary directory
temp/                   # Temporary directory
*.log                   # Log files (general)

# 
# TESTING AND COVERAGE
# ====================
# Test output and coverage reports (if you add testing later).
#
.coverage/              # Coverage reports
.nyc_output/            # NYC test coverage
coverage/               # Coverage directory
*.lcov                  # LCOV coverage files

# 
# PACKAGE MANAGER FILES
# =====================
# Lock files - these are typically committed, but can be excluded
# if you want to allow flexibility in dependency versions.
# NOTE: yarn.lock and package-lock.json are usually committed.
#
# package-lock.json     # NPM lock file (usually committed)
# yarn.lock             # Yarn lock file (usually committed)

# 
# BUILD ARTIFACTS
# ===============
# Compiled and minified files that are generated from source.
# NOTE: For this project, we DO commit assets/css/main.css and
# assets/js/main.js because GitHub Pages needs them. If you want
# to generate these only during deployment, uncomment the lines below.
#
# assets/css/main.css   # Compiled CSS (if generating on deploy)
# assets/js/main.js     # Compiled JS (if generating on deploy)

# 
# MISC
# ====
# Other files that should be ignored.
#
*.orig                  # Merge conflict files
*.rej                   # Rejected patch files
.directory              # Directory metadata
node_modules
